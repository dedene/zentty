{
  "name": "@zentty/companion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zentty Mobile Companion \u2014 JS workspace root (wire, relay, mobile).",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20"
  }
}
